# IRIS Teensy USB device rules — persistent symlinks by hardware serial number.
# These rules survive port swaps and reboots.
#
# Deploy: sudo cp 99-iris-teensy.rules /etc/udev/rules.d/
#         sudo udevadm control --reload-rules && sudo udevadm trigger
#
# Result:
#   /dev/ttyIRIS_EYES  -> Teensy 4.1 (TeensyEyes + TFT mouth) — serial YOUR-T41-SERIAL
#   /dev/ttyIRIS_SERVO -> Teensy 4.0 (servo + PAJ7620 gesture) — serial YOUR-T40-SERIAL
#
# Identification is by USB hardware serial number (ATTRS{serial}), NOT by port
# position. Symlinks survive cable moves and reboots.
#
# Note (S97): serial numbers re-confirmed by connecting T41 alone and observing
# which serial appeared. S94b had these swapped — corrected here.
# T41 serial YOUR-T41-SERIAL confirmed: [VER] S97, FACE:N, EMOTION/EYE/MOUTH responses.
# T40 serial YOUR-T40-SERIAL confirmed: DIAG pan=N pajOk=N, gesture commands (VOL+, STOP, etc.)

SUBSYSTEM=="tty", ATTRS{idVendor}=="16c0", ATTRS{serial}=="YOUR-T41-SERIAL", SYMLINK+="ttyIRIS_EYES"
SUBSYSTEM=="tty", ATTRS{idVendor}=="16c0", ATTRS{serial}=="YOUR-T40-SERIAL", SYMLINK+="ttyIRIS_SERVO"
